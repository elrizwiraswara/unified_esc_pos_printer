<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elriztechnology.unified_esc_pos_printer">

    <!--
        This package intentionally declares no permissions.

        Permissions must be declared by the consuming app in its own
        AndroidManifest.xml so that the app can choose the correct
        maxSdkVersion gating for its use case. See the package README
        ("Android Setup") for the recommended permission set.
    -->
</manifest>
